"""ctgov: trial reader + cache, NCT -> TargetTrialSpec, and the trial ->
ExtractionPlan 'intelligence' (#1/#2/#3, owner: probe).

Stub — built via PR onto this seed. Emits contracts.TargetTrialSpec and
contracts.ExtractionPlan. Reuses trialsim fetch_ctgov.py + emulaTTE schemas.
"""
